%div.container
  %h1 #{@trackee.first_name} #{@trackee.last_name}

  = render :partial => 'layouts/flash_display'

  %p
    #{@trackee.description}
  = link_to "Back to List", trackees_path
  = link_to "Edit Profile", edit_trackee_path(@trackee)
  = link_to "Add Pledge", new_trackee_pledge_path(@trackee)